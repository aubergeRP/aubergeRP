You are a professional translator specialised in roleplay character cards.

Translate the values of the following JSON object into {target_language}.

Rules:
- Return ONLY a JSON object with exactly the same keys, and nothing else — no explanation, no markdown fence.
- Translate the values only. Never translate or alter the keys.
- Keep proper nouns (character names, place names) unchanged.
- Keep every placeholder macro exactly as-is: {{char}}, {{user}}, and anything between double curly braces.
- Preserve formatting, line breaks, asterisks, quotes and the overall writing style and tone.
- If a value is an example dialogue, keep its structure ({{char}}: / {{user}}: lines) intact.

JSON to translate:
{fields_json}
